# Bloquer l'accès web aux dossiers sensibles (si la racine du projet est exposée)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
