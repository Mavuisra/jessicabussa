# Ce fichier est chargé automatiquement sur le serveur LWS.
# Priorité : .env.production (base) puis .env (surcharges).
# Uploadez .env.production à la racine du site — le renommer en .env est optionnel.

APP_ENV=production
APP_DEBUG=false
APP_URL=https://www.jessicabussa.cd

DB_DRIVER=mysql
# Sur LWS : utiliser 127.0.0.1 (localhost force une socket Unix inaccessible)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=lasav2675681_12lmwo
DB_USER=lasav2675681
DB_PASSWORD=

# Email Netim (boîtes mail — indépendant de l'hébergement LWS)
MAIL_HOST=mail1.netim.hosting
MAIL_PORT=465
MAIL_ENCRYPTION=ssl
MAIL_USERNAME=contact@jessicabussa.cd
MAIL_PASSWORD=
MAIL_FROM=contact@jessicabussa.cd
